# StudentMarks
Allows user to enter in a student ID and that students mark and then store it into a text file. The user can also delete or edit existing marks. If the user wants to they can filter the grades by Max, min or average
